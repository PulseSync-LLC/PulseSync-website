import Dashboard from '@/components/layout/dashboard';
import { ReactNode } from 'react';

const SubscriptionPage: React.FC = () => {
    return (
        <Dashboard title="Подписка" description="Информация о подписке">
            <div>
                <h1>Подписка</h1>
                <p>В разработке.</p>
            </div>
        </Dashboard>
    );
};

export default SubscriptionPage;
